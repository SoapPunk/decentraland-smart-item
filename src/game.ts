import { Spawner } from './spawner'
////////////import Button, { Props } from './_item'
////////////
////////////
////////////const button = new Button()
////////////const spawner = new Spawner<Props>(button)
////////////
////////////spawner.spawn(
////////////'button',
////////////new Transform({
////////////  position: new Vector3(4, 0, 8)
////////////})
////////////)
////////////